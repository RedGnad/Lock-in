[profile.default]
src = "contracts"
test = "test"
script = "script"
out = "out"
libs = ["node_modules", "lib"]
auto_detect_solc = true
evm_version = "shanghai"
optimizer = true
optimizer_runs = 200
via_ir = true

[rpc_endpoints]
monad = "${MONAD_RPC_URL}"
